const schedule = require('node-schedule');

/* const jobRule = new schedule.RecurrenceRule();
jobRule.minute = 30;

const job = schedule.scheduleJob(jobRule, function(){
  console.log('Minute 30, Revoking jobs');
  
  const quests = [
    {
      from: "Fisherman",
      goals: "Catch 5 fish",
      objective: [
        {
          "Fish": 5,
        }
      ]
    }
  ]
}); */

module.exports = {
  
}